# Husky_viz — Clearpath Husky A200 Simulator

Working notes and source of truth for the Husky simulation on this machine.
Everything below was verified empirically on 2026-08-26, not copied from docs.

## Repository layout — and where new things go

**This folder is the source of truth. `~/clearpath/` is a build output.**

```
Husky_viz/
  CLAUDE.md            these notes
  robot_configs/       robot_*.yaml — the only place configs are edited
  worlds/              custom .sdf worlds
  urdf/                custom xacro injected via platform.extras.urdf
  launch/              custom launch files
  scripts/             executable entry points
  tools/               rclpy verification scripts (check_*.py)
```

| Adding... | Put it in | Notes |
|---|---|---|
| a robot config | `robot_configs/robot_<name>.yaml` | apply with `scripts/apply_config.sh <name>` |
| a world | `worlds/*.sdf` | world-level Gazebo systems live here |
| a custom sensor xacro | `urdf/` | then reference it from `urdf/extras.urdf.xacro` |
| a launch file | `launch/` | |
| a shell entry point | `scripts/` | `chmod +x` |
| a check/verification script | `tools/check_<what>.py` | name it after what it proves |
| a nav2 / localization config | `config/` | not `robot_configs/`, which is Clearpath robot descriptions |
| a generated map or mask | `maps/` | regenerate with `tools/generate_park_maps.py` |
| a waypoint route | `routes/` | lat/lon valid only against the world's datum |
| generated URDF/params/launch | nowhere — they land in `~/clearpath/` | **never edit or commit these** |

`~/clearpath/` holds `robot.yaml` (a deployed copy) plus generated
`robot.urdf.xacro`, `platform/`, `sensors/`, `manipulators/`. All of it is
disposable and regenerated by `apply_config.sh`. Editing anything there is a
mistake — the change is silently overwritten on the next apply.

Cross-file references use **absolute paths into this folder**
(`/home/thinhpham/Documents/Husky_viz/...`), because Clearpath's generator
resolves `platform.extras.urdf` relative to nothing useful. If this folder is
ever moved, fix paths in `robot_configs/robot_full.yaml`,
`urdf/extras.urdf.xacro`, and `scripts/run_husky_sim.sh`.

`~/run_husky_sim.sh` is a symlink to `scripts/run_husky_sim.sh`.

## Environment

| Item | Version |
|---|---|
| Ubuntu | 24.04 Noble |
| ROS 2 | Jazzy (`/opt/ros/jazzy`) |
| Gazebo | Harmonic 8.11.0 (vendored via `gz-*-vendor`) |
| `clearpath_simulator` | 2.9.4 |
| `clearpath_common` / `clearpath_config` | 2.9.15 / 2.9.6 |
| GPU | NVIDIA Quadro P4000 Mobile |

Installed from **apt binaries** (`ros-jazzy-clearpath-simulator`), NOT built from
source. The upstream README's `colcon build` workspace is unnecessary — there is
no `~/clearpath_ws` and nothing to rebuild after upgrades.

`gz` is not on the global PATH; it resolves only after
`source /opt/ros/jazzy/setup.bash`, because Gazebo is vendored inside the ROS tree.

## Workflow

**Two runbooks hold the procedure. Both contain steps only, no explanation, and
are maintained for demos — follow them verbatim rather than improvising, and if
a step fails, fix the file rather than working around it.**

| File | Covers | Skill |
|---|---|---|
| `CLEAN_SIM.md` | kill survivors, clear `/dev/shm`, verify clean | `husky-sim-restart` |
| `RUN_SIM.md` | launch, verify topics, verify the robot landed | `husky-run-sim` |
| `NAV_PARK.md` | launch nav2 + GPS localization in park, send a goal, verify arrival and local avoidance | — |
| `DEMO.md` | demo scenarios, one self-contained block each | — |

`CLEAN_SIM.md` runs first and must report `opt/ros : 0` / `shm : 0` before
`RUN_SIM.md` Step 3. This section covers the surrounding context; the runbooks
cover the doing.

**Never run sim commands from the main conversation. Route every sim
operation — start, stop, restart, verify, demo — to the `sim-operator` agent**
(`.claude/agents/sim-operator.md`). It loads both skills, reads the runbooks
itself, and returns a compact per-gate report. Give it the goal ("start lake"),
not the commands; check its report; if something is wrong, tell it to fix and
re-run. Demo commands come from the project's docs, never from the caller.

```bash
cd ~/Documents/Husky_viz

./scripts/apply_config.sh default   # wheel odom + IMU + 2D/3D lidar
./scripts/apply_config.sh full      # + camera, compass, radio

~/run_husky_sim.sh                     # warehouse_ext (compass + radio)
~/run_husky_sim.sh warehouse_ramp      # + 15 deg ramp for terrain tests
~/run_husky_sim.sh warehouse_ext true  # with rviz

python3 tools/check_all_sensors.py     # confirm every stream is live
```

`apply_config.sh` prints which sensors survive into SDF after regenerating —
that is the only reliable check that a declared sensor will actually publish.

**Never wait on a sim launch with a fixed sleep, and never delegate the check
to a script that loops/sleeps internally either — that is the same thing
wearing a different hat. Check readiness yourself, directly, with one-shot
commands, and report immediately:**

```bash
ros2 topic list 2>/dev/null | grep -E "platform/odom|imu_0/data|lidar2d_0/scan|lidar3d_0/points"
ros2 topic info -v /a200_0000/sensors/imu_0/data 2>/dev/null | grep -A1 "Publisher count"
```

A topic in `ros2 topic list` only means discovery found it, not that it is
publishing — confirm with `Publisher count: 1` on the topics that matter for
the task at hand. If not ready yet, say so and check again with another
one-shot command; do not sleep, do not poll in a loop, do not write a wrapper
script for this. The user has zero tolerance for being made to wait on a
check that could have been instant. `tools/wait_for_ready.py` was tried and
is explicitly rejected as a pattern - delete the instinct to reach for it.

Stock Clearpath worlds (`warehouse`, `construction`, `office`, `orchard`,
`pipeline`, `solar_farm`) work via
`ros2 launch clearpath_gz simulation.launch.py world:=<name>` but have **no
compass and no radio** — those are world-level Gazebo systems present only in
`worlds/warehouse_ext.sdf` and `worlds/warehouse_ramp.sdf`.

## Configs

**`robot_default.yaml`** — wheel odometry, IMU, GPS, 2D + 3D lidar. Verified live:

| Stream | Rate |
|---|---|
| wheel odom | 11.8 Hz |
| odom (EKF fused: wheel + IMU) | 33.2 Hz |
| IMU | 67.2 Hz |
| GPS (`navsat`) | 2 Hz (custom `urdf/gps_2hz.urdf.xacro`, replacing the 1 Hz stock `garmin_18x`) |
| 2D lidar (`gpu_lidar`) | 23.7 Hz |
| 3D lidar (`gpu_lidar`) | 13.0 Hz |

Wheel odometry needs no sensor entry — the diff-drive controller publishes
`platform/odom`, and the generated `robot_localization` EKF fuses it with the IMU
into `platform/odom/filtered`.

**GPS is fully built-in — unlike the compass, nothing had to be written.**
`garmin_18x` (model `navsat`) ships with a `<gazebo>` block in
`clearpath_sensors_description`, the stock worlds already load the `NavSat`
system plugin and define `<spherical_coordinates>`, and Clearpath's generator
auto-bridges it. Adding GPS is purely a `robot_configs/` edit — no custom
xacro, no world patch, no manual `ros_gz_bridge` line. It is mounted on
`sensor_arch_mount` with `xyz: [0.0, 0.15, 0.0]` to sit high and clear of the
3D lidar.

Verified against the warehouse world: `latitude -22.98668564552283`,
`longitude -43.20250099999985` — exactly the world's `<spherical_coordinates>`
origin (Rio de Janeiro), i.e. a real georeferenced fix, not zeros. The GPS
position is therefore only meaningful relative to whatever that world declares
as its datum; a world without `<spherical_coordinates>` yields no useful fix.

Other sim-ready GPS models (all `navsat`, all drop-in): `novatel_smart6`,
`novatel_smart7`, `swiftnav_duro`, `microstrain_gq7`, `fixposition`.

**`robot_full.yaml`** — `default` plus RealSense D435 camera, compass, RF radio.
Requires `worlds/warehouse_ext.sdf` for the compass/radio to function.

**`default` is the config to use unless a task explicitly asks for `full`.**
`RUN_SIM.md` Step 2 enforces this. `full` is strictly a superset — it had
silently *dropped* GPS until that was caught by `RUN_SIM.md` Step 4 failing, so
if you add a sensor to `default`, add it to `full` too or the superset claim
quietly stops being true.

## Ported worlds — `park`, `lake`

Ported from the ROS 1 `natural_environments_ros_opt` dataset. This is a **Gazebo
Classic 11 → Harmonic** migration, not ROS 1 → ROS 2: worlds are Gazebo's format
and ROS never reads them, so every incompatibility was a simulator one. Model
placements come from each source world's `<state>` block (gotcha #16), verbatim.

| | park | lake |
|---|---|---|
| models | 97 | 79 |
| ground z | ≈2.99 (flat) | 3.5–5.9 (relief 2.43 m, max slope 21°) |
| spawn (x y z yaw) | `45.64 0.02 3.3 2.6132` | `-47 -15 4.0 0` |
| datum | 49.9 N, 8.9 E, `heading_deg` 90 (source dataset, since 2026-08-26) | lat/lon 0 (source) |
| orientation | +x North, +y West; spawn 4.36 m inside the north edge heading southwest, ~95 m of run south | — |
| compass/radio | **no** | **no** |

park's datum was Rio (user choice) before 2026-08-26; any GPS fix recorded
before that date is invalid against the current world. lake still uses Rio.

Neither carries `Magnetometer`/`RFComms`, so `robot_full.yaml`'s compass and
radio are silent there; every other sensor works. Only `warehouse_ext` /
`warehouse_ramp` have those systems — and note `warehouse_ramp` contains only a
`ground_plane`, so lake is the first surface in this project with real slopes.

Meshes live in `models/` — real copies, never symlinks (a dead symlink into
`/home/thinh/...` broke the first attempt). Visual and collision geometry point
at `_lowpoly` variants that already shipped with the assets: park went 161.6 MB
→ 27.4 MB of referenced mesh data and 83 s → 4 s load. "Low-poly" here still
means ~40k triangles; lake carries 3.8 M collision triangles total (64 bushes
account for 2.26 M of it). park loads in ~4 s.

**Real-time factor is set by the robot config, not by the world.** Measured on
lake: **RTF ~1.0 under `default`**, **0.10–0.14 under `full`** — identical world,
identical 3.8 M collision triangles, the only difference being `full`'s RealSense
camera rendering colour + depth + a point cloud every step. Collision geometry
was blamed for this first and was wrong; the triangle count never changed while
RTF went 0.12 → 1.0. Do not decimate foliage collision to chase framerate — drop
the camera, or accept the cost when you need it.

Low RTF is a presentation trap when it does occur: the robot's 1.0 m/s cap is
*simulated* speed, so at RTF 0.12 it advances ~12 cm per wall-clock second.
Things look stuck when they are merely slow — measure displacement against sim
time before diagnosing a traction or control fault.

**GUI framerate is not RTF.** The renderer draws at ~60 fps regardless of how
fast simulated time advances, so motion looks smooth at any RTF. Read
`real_time_factor` from `/world/<name>/stats`; the fps counter says nothing about
simulation speed.

Textures remain a load-time cost (~221 MB referenced in park, including a single
46 MB normal map on a bench instantiated 16 times) — that affects startup, not
RTF.

**Deviations from source.** Both use `max_step_size 0.003` so the verified rate
table applies. lake has `<shadows>0</shadows>` (source said 1) and an 8192×8192
RGB JPEG terrain texture replacing the 8000×8000 RGBA PNG — both were wrong
diagnoses of the black-terrain bug (the real cause was gotcha #19), harmless,
revertible. lake's `arbusto3` collision uses the low-poly mesh, whose bbox is
~6.5× smaller than the source's — the original had 34 bushes with invisible
oversized collision volumes.

## Namespace and topics

Namespace `a200_0000` (from `serial_number: a200-0000`).

| Stream | Topic |
|---|---|
| Command velocity | `/a200_0000/cmd_vel` — **`geometry_msgs/TwistStamped`** |
| Wheel odometry | `/a200_0000/platform/odom` |
| EKF fused odom | `/a200_0000/platform/odom/filtered` |
| IMU | `/a200_0000/sensors/imu_0/data` |
| GPS | `/a200_0000/sensors/gps_0/fix` — `sensor_msgs/NavSatFix` |
| 2D lidar | `/a200_0000/sensors/lidar2d_0/scan` |
| 3D lidar | `/a200_0000/sensors/lidar3d_0/points` |
| Camera | `/a200_0000/sensors/camera_0/color/image`, `.../depth/image`, `.../points` |
| Compass | `/a200_0000/sensors/compass_0/mag` |
| Radio | `/broker/msgs` (send), `/husky/rx`, `/base_station/rx` |

Subscribe with sensor-data QoS (`qos_profile_sensor_data`).

## Sim-ready sensor models (drop-in via `robot_configs/`)

- Cameras: `intel_realsense`, `flir_blackfly`, `luxonis_oakd`, `stereolabs_zed`
- 2D lidar: `hokuyo_ust`, `sick_lms1xx`
- 3D lidar: `velodyne_lidar`, `ouster_os1`, `seyond_lidar`
- IMU: `microstrain_imu`, `phidgets_spatial`, `chrobotics_um6`, `redshift_um7`
- GPS/NavSat: `garmin_18x`, `novatel_smart6/7`, `swiftnav_duro`,
  `microstrain_gq7`, `fixposition`
  (**not** `nmea_gps` — accepted by the config schema but has no xacro at all,
  so it silently never publishes; same trap as gotcha #1)

All lidars are **GPU** (`gpu_lidar`), never CPU `ray`.

**Not wired: thermal camera.** Gazebo supports it (verified: a 500 K box read
back 501.15 K against 287.96 K ambient), but Clearpath ships no thermal model, so
it needs a custom xacro + world plugin + manual bridge, same as the compass.

## Gotchas

**1. Four sensor models silently never publish.** `axis_camera`, `flir_ptu`,
`gnss_antenna`, `wiferion` have no `<gazebo>` block. Config accepts them, URDF
builds, robot looks right, no topic ever appears. Use `flir_blackfly` or
`luxonis_oakd` instead of `axis_camera`. This is why `apply_config.sh` prints the
SDF sensor list.

**2. `sensors/imu_0/data_raw` never publishes in sim.** It is advertised by the
bridge but nothing feeds it. On hardware `imu_filter_madgwick` consumes
`data_raw` and emits `data`; in Gazebo the sensor writes straight to `data`.
**Always read `.../data`.**

**3. `cmd_vel` is `TwistStamped`, not `Twist`.** Most teleop tutorials are wrong
for this stack.

**4. The Gazebo model is named `a200_0000/robot`**, not `a200-0000` (which is
what appears in SDF parse warnings). `/world/warehouse/set_pose` returns
`data: true` even for a nonexistent entity, so a wrong name fails silently.

**5. Magnetometer magnitude is 10^4x too large** when the world has
`<spherical_coordinates>` — a Gauss/Tesla bug in Gazebo. Direction is correct (a
real local field for the world's lat/lon), so heading works; divide by `1e4`
before trusting `|B|`. Verified: 0.24 T with spherical coords, correct
4.849e-05 T without.

**6. `simulation.launch.py` hard-restricts `world` to six names.** Custom worlds
need `launch/sim_compass.launch.py`, which accepts an absolute path (minus the
`.sdf`, which the launch appends).

**7. Custom sensors are never auto-bridged.** Clearpath's generator only bridges
models it knows. Compass and radio ride on the explicit `ros_gz_bridge` call in
`scripts/run_husky_sim.sh`; anything custom added later needs a line there too.

**8. Killing a `ros2` CLI command with `timeout` trips Ubuntu's apport dialog**
("ros2 has stopped unexpectedly"). Cosmetic — the CLI does not unwind on SIGTERM.
Prefer `ros2 topic pub -t N` (exits cleanly) or a small rclpy script.

**9. Do not `pkill -f` with a pattern that appears in your own command line** —
it matches the invoking shell and kills it.

**10. `set -u` breaks `/opt/ros/jazzy/setup.bash`** (unbound `AMENT_TRACE_SETUP_FILES`).
Use `set -eo pipefail` in scripts that source ROS.

**11. `ros2 launch` leaves orphaned nodes behind if you kill the launch process
by PID instead of the process group.** Killing `gz sim`/`clearpath_gz` matches
does NOT kill the `static_transform_publisher`, `robot_state_publisher`, etc.
child nodes `ros2 launch` spawned — they detach and keep running, still bound
to the DDS bus. **Always kill the full process tree before relaunching:**

```bash
for p in $(pgrep -f "static_transform_publisher|gz sim|clearpath_gz|sim_compass|parameter_bridge|rviz2|robot_state_publisher|controller_manager|ekf_node|twist_mux|imu_filter"); do
  grep -qa "bash -c" /proc/$p/cmdline 2>/dev/null || kill -9 $p 2>/dev/null
done
```

Verify with `pgrep -af "gz sim|static_transform_publisher"` before relaunching —
it must print nothing.

**12. `kill -9` on a ROS/DDS process leaves its FastDDS shared-memory segments
locked in `/dev/shm`, and that is what actually causes intermittent,
sensor-specific failures on the next launch — not the orphaned processes in
#11 by themselves.** SIGKILL gives no chance to release the segment; the next
process that tries to bind the same shared-memory port hits
`RTPS_TRANSPORT_SHM Error: Failed init_port ...: open_and_lock_file failed`,
and which topic loses data is essentially random. Confirmed on this machine:
after a session of repeated `kill -9`, `/dev/shm` held **488** stale
`fastrtps_*` segments, and relaunching on top of them made camera and compass
silently produce no data while lidar/IMU/odom kept working — twice, even
after fully clearing the process tree per #11 alone. Clearing `/dev/shm` (not
just the processes) was what actually fixed it:

```bash
rm -f /dev/shm/fastrtps_* /dev/shm/sem.fastrtps_* 2>/dev/null
```

`scripts/run_husky_sim.sh` now does both #11's process kill and this cleanup
automatically before every launch. Do the same by hand before using
`ros2 launch clearpath_gz simulation.launch.py` directly, since that path has
no such guard.

**A nonzero segment count read immediately after the kill is usually transient,
not a leak.** Participants release their segments as they tear down, so counting
too early catches them mid-release — observed at `38` twice, both times dropping
to `0` seconds later unaided. Re-read once (no sleep, just run the count again):
drops to zero means it was clearing; stays nonzero means something is alive and
recreating them, which is a real leak to find by process name. Do not `rm` a
second time to force the number down — that hides the live process rather than
fixing it. `CLEAN_SIM.md` Step 3 encodes this.

**13. `run_husky_sim.sh` re-applies the robot config at launch, so it — not
whatever you last ran `apply_config.sh` with — decides which sensors exist.**
The re-apply is deliberate: `clearpath_gz` regenerates the URDF from
`~/clearpath/robot.yaml` at startup (`generate:=true` by default), so without it
the sim silently spawns whatever config was last deployed, possibly by an
unrelated run. Nothing errors, and lidar/IMU look fine because they exist in
both configs, so a missing camera or GPS is easy to miss.

It defaults to `default`; select the full suite with `SIM_CONFIG=full
~/run_husky_sim.sh <world>`. It was hardcoded to `full` until 2026-08-26, which
silently overrode `RUN_SIM.md` Step 2 — applying a config in Step 2 alone does
not survive Step 3. The launch log prints which config was applied; check it
rather than assuming Step 2 won.

**14. A subscriber that starts right as the sim comes up can miss the
discovery window and read a live topic as "NO DATA" for a few seconds — this
is a false negative in the checking script, not a broken sensor.** Hit twice:
`check_all_sensors.py` reported `odom: 0.0 Hz` immediately after launch while
every other stream was fine; a fresh one-shot `ros2 topic info -v` check
seconds later showed `Publisher count: 1` and a plain subscriber read
`9.6 Hz`, with zero changes to the sim. Don't diagnose a sensor from one
`check_*.py` script's single sampling window right after launch. Confirm with
a direct one-shot command instead (see Workflow section) before trusting a
"NO DATA" result.

**15. The GPS ROS topic is `gps_0/fix`, not `gps_0/navsat`.** The xacro's
Gazebo `<topic>` says `.../sensors/gps_0/navsat`, but Clearpath's generated
bridge remaps it to `/a200_0000/sensors/gps_0/fix` (`sensor_msgs/NavSatFix`).
Grepping `ros2 topic list` for "navsat" finds nothing and looks like a broken
sensor — search for `gps` instead.

**16. In a Gazebo Classic world the `<state world_name='...'>` block holds the
authoritative poses — the per-model `<pose>` values are stale authoring
leftovers.** Classic applies `<state>` over them at load. Deleting the state
block as "Classic-only, every model carries its own pose" silently scatters the
world: in park it left the terrain at z=0 while 89 props sat at z≈3; in lake 40
of 79 poses were wrong, shifted by one model, in all six components. Transfer
the `<state>` poses onto the models before deleting it —
`tools/restore_world_state_poses.py`.

**17. Harmonic aborts the ENTIRE world load if any mesh URI cannot be resolved**
(`Error Code 14` → `Error Code 9: Failed to load a world`). Classic warned and
rendered everything else. Audit `model://` URIs *and* bare absolute paths —
park.world carried `/home/a/Desktop/...` from the original author's machine.

**18. A collision mesh with zero vertex normals segfaults DART/ODE.** gz-sim
logs "this submesh will be ignored" and hands it to DART anyway, which walks the
absent normals array and dies in `OdeMesh::fillArrays`. The warning is false.
`tools/add_obj_vertex_normals.py` — note OBJ faces must *index* the normals
(`f 1//1`), not merely have `vn` lines.

**19. Ogre `<script>` materials: built-in `Gazebo/*` names resolve, custom ones
do not.** gz-sim's internal `MaterialParser` looks built-ins up by name in its
own bundled `gazebo.material` and ignores the `<uri>`; a `model://`-scoped
script hits "Custom material scripts are not supported" and renders untextured.
Convert to `<pbr>` — **and include `<diffuse>1 1 1 1</diffuse>`**. SDF defaults
base colour to (0,0,0) and ogre2 multiplies the albedo map by it, so a `<pbr>`
block without `<diffuse>` renders pure black regardless of the texture.

**20. Diagnose rendering problems by querying the live renderer, not by diffing
world files.** `gz service -s /world/<name>/scene/info --reqtype gz.msgs.Empty
--reptype gz.msgs.Scene --timeout 30000 --req ''` dumps the material as the
renderer holds it. Protobuf omits zero fields, so `diffuse { a: 1 }` means
r=g=b=0. Two wrong diagnoses (texture size, shadow setting) preceded one query
that settled it.

**21. `scripts/kill_sim.sh` verifies with the same pattern list it kills with,**
so it cannot detect what it does not kill — it printed `clean: no sim processes
remain` with 73 alive. The list has been incomplete twice (Clearpath's teleop
stack `marker_server`/`joy_linux`/`teleop_twist_joy`, then
`ros_gz_image/image_bridge`). Sweep on the `a200_0000` namespace and verify with
a broader, independent check. See the `husky-sim-restart` skill.

**22. Launch with `setsid nohup ... &` then `disown`.** A plain `nohup ... &`
stays in the caller's process group, so interrupting the invoking command sends
SIGINT to Gazebo; it exits cleanly mid-load and reads as a crash or hang
(`Received signal[2]` … `process has finished cleanly`).

**23. The spawn pose must match the world's ground height.** Clearpath defaults
to `z=0.15`; park's ground is at z≈2.99 and lake's spans 3.5–5.9, so the default
spawns the robot *under* the terrain and it falls forever (observed z=-12116).
Authored poses live in the original ROS 1 launch files
(`natural_enviroment/launch/add_husky_<world>_1.launch`), wired per-world in
`scripts/run_husky_sim.sh` with `SPAWN_X/Y/Z/YAW` overrides.

**24. ROS 2 Clearpath replaces wheel friction with a `WheelSlip` plugin, and it
overwrites whatever `<mu>` the terrain or wheels declare, every timestep.** So
the natural place to look for a traction bug is the one place that cannot fix
it. Stock `slip_compliance_lateral/longitudinal` is `1.0` — tuned for flat
indoor floors, and it makes the robot drift downhill on any grade (measured
0.198 m sideways per metre travelled on a 5° slope; it then slides off the edge
of the world). ROS 1's `husky_description/urdf/wheel.urdf.xacro` used hard
Coulomb friction (`mu1/mu2 1.0`, `kp 1e7`, `kd 1.0`, `fdir1 "1 0 0"`) and **no
slip model at all**, which is why this never happened there.
`urdf/wheel_slip.urdf.xacro` restores that: compliance `0.0` both axes
(`DART_DEFAULT_SLIP_COMPLIANCE` is 0.0, so zero is rigid, not "unset"), giving
0.0045 m/m — 44× better. `kp`/`kd` reach the SDF but DART is a rigid LCP solver
and ignores them.

**25. Neither ported world has a ground plane.** The terrain mesh is the only
surface (`terreno_lago` spans x ±49.8, y ±24.9). Drive past its edge and there
is nothing to land on. True of the original worlds too. lake's spawn at `y=-15`
is only 9.9 m from the boundary, so a robot that "vanishes" has usually left the
terrain — check `gz model -m a200_0000/robot -p` for a large negative z before
assuming anything broke.

**26. Gazebo services are scoped by world name**, so they are `/world/park/...`
and `/world/lake/...`, not `/world/warehouse/...`. Every `tools/check_*.py` that
teleports the robot has `warehouse` baked in, and per gotcha #4 `set_pose`
returns `data: true` regardless — so those tools silently no-op in the ported
worlds rather than erroring. Parameterise the world name before trusting a
teleport-based check outside warehouse.

**27. The controller spawner race can fail outright, not just be slow, and
at either controller.** Activation was measured at **4.59 s** against a hard
**5.00 s** switch timeout, so it fails roughly half the time and presents as
a robot that will not move with every `RUN_SIM.md` gate green. Observed
twice on 2026-08-26: once losing at `platform_velocity_controller`, once at
`joint_state_broadcaster` before it ever reached the second controller.
`RUN_SIM.md` Step 4 originally only checked `imu_0/data`, which is published
straight from the Gazebo sensor and is structurally blind to a dead
controller spawner — a sim has passed every gate while
`platform_velocity_controller` was absent and `/a200_0000/platform/odom` had
zero publishers. Step 4 now also requires:
```bash
ros2 service call /a200_0000/controller_manager/list_controllers controller_manager_msgs/srv/ListControllers "{}"
```
with both `joint_state_broadcaster` and `platform_velocity_controller` state
`active` (the `ros2 control` CLI is **not installed** on this machine, so the
service call is the working form), plus
`ros2 topic info -v /a200_0000/platform/odom` requiring `Publisher count: 1`.
If that gate fails, `RUN_SIM.md`'s recovery step (re-running the spawner
manually for both controllers with `--switch-timeout 30`) is **UNTESTED** —
the failure has never been reproduced on demand.

**28. The prior park map deliberately omits the 15 `arbol4` small trees**
(3.10 m across, 4.11 m tall) so the local costmap has to avoid them from live
lidar (`tools/generate_park_maps.py`). A tree missing from
`maps/park_map.pgm` is intended, not a generator bug. `tree_8` (12.9 m) and
all man-made objects are mapped.

**29. park's 16 benches sit entirely ~0.5 m below the terrain surface.** They
are invisible to lidar and to physics, and the robot drives straight through
them. True of the original ROS 1 world too.

**30. The keepout mask is the only thing stopping nav2 planning off the
terrain.** A lidar ray past the edge returns max range, which the costmap
reads as free space and actively *clears*. The void cannot be sensed (see
gotcha #25 — neither ported world has a ground plane).

**31. Nav2 costmap filter and static-layer topics must be absolute.**
Relative names resolve inside the costmap node
(`/a200_0000/global_costmap/...`) and silently never bind — the filter logs
`Filter mask was not received` and the prior map is simply absent.

**32. park's datum changed on 2026-08-26** from Rio to the source dataset's
49.9 N, 8.9 E, and `heading_deg` is now 90, so +x is North and +y is West.
Any GPS fix recorded before that date is invalid. The warehouse worlds still
use Rio.

**33. The `ros2 control` CLI is not installed on this machine.** Query
controller state via the `controller_manager` service directly (see gotcha
#27) rather than reaching for `ros2 control list_controllers`.

**34. Nav2 comes up healthy but useless if started before `map -> odom`
exists.** No crash, no error, goals silently ignored. park's GPS is 2 Hz so
the window is still wide. Always gate with `tools/check_nav2_ready.py`.

**35. COLLADA up-axis is mixed in this dataset.** `arbol4/*` and `bench/*`
are `Y_UP`; the rest are `Z_UP`. A loader that ignores it puts 31 of 97
models on their side, and the resulting map still looks plausible.

**36. Costmap `observation_sources` topics must be absolute too.** A costmap
layer resolves a relative observation-source name *inside the costmap sub-node*,
so `topic: sensors/lidar2d_0/scan` bound to
`/a200_0000/local_costmap/sensors/lidar2d_0/scan` (and the `global_costmap`
equivalent) — names with zero publishers. Nothing errors: the layers come up
enabled and simply never mark. Verified live 2026-08-26 in park — both costmaps
had received no scan at all while the lidar showed 50 returns on a tree trunk
0.2 m ahead, i.e. obstacle avoidance was silently inert. Fixed in
`config/nav2_park.yaml` by writing `/a200_0000/sensors/lidar2d_0/scan`. Same
trap as #31, one layer type further in.

**`collision_monitor` is NOT affected** — it is an ordinary node, not a costmap
sub-node, so its relative `sensors/lidar2d_0/scan` correctly resolves to
`/a200_0000/sensors/lidar2d_0/scan` (confirmed: exactly 1 subscriber on that
topic). Leave it relative.

## Sensor semantics (verified)

**IMU mounting is correct.** `base_link -> imu_0_base_link` (identity)
`-> imu_0_link` (+2.5 mm z), no rotation. Measured: gravity `[0, 0, +9.800]` at
rest; CCW yaw command gives `gyro.z = +0.488` for `+0.6 rad/s`; forward command
gives `accel.x = +7.00`. Right-handed, z-up, REP-103 / ENU compliant.
See `tools/check_imu_orientation.py`.

**IMU contains a gyroscope; it is not one.** `angular_velocity` is the true gyro
(rate, rad/s). `orientation` is attitude (angle). At rest facing 123 deg the gyro
reads `0.0000` while orientation reads `+123.3 deg`. Integrating the gyro over a
4 s turn gave `+123.3 deg` vs orientation delta `+123.3 deg` — 0.0 deg apart.
See `tools/check_gyro_vs_orientation.py`.

**That perfect agreement is a sim artifact.** Gazebo supplies `orientation` as
ground truth from the physics engine; it never integrates and never drifts. Do
NOT validate an AHRS/EKF against sim `orientation` — real gyro drift is exactly
the failure mode you would be trying to catch.

**IMU attitude is gravity-relative, not terrain-relative.** On a 15 deg ramp it
reads pitch `-14.86 deg` (0.14 deg error) while the robot's tilt relative to the
ramp surface is 0 deg — it sits flush. The IMU cannot distinguish "on a slope"
from "tipping over on flat ground". Body attitude equals ground slope only
because the A200 is a rigid 4-wheel chassis with all wheels in contact; that
assumption breaks on a rock, ridge, or hole. For true terrain-relative tilt, fit
a plane to `lidar3d_0/points` or `camera_0/points` and compare its normal against
the IMU gravity vector. See `tools/check_terrain_tilt.py`.

**Compass is not an IMU and not a gyroscope.** `sensor_msgs/MagneticField` has one
field — a 3-vector. No orientation, no rates. Heading is something you compute:
`atan2(-y, x)`. Verified: rotating 90 deg moved heading `+24.0 -> +114.0`. Tilt
corrupts it — 14.4 deg of pitch introduced 4.4 deg of heading error — so
tilt-compensate with IMU roll/pitch:

```python
Xh = mx*cos(pitch) + my*sin(roll)*sin(pitch) + mz*cos(roll)*sin(pitch)
Yh = my*cos(roll)  - mz*sin(roll)
heading = atan2(-Yh, Xh)
```

See `tools/check_compass_heading.py`.

**Radio (RFComms)** is log-distance path loss. Verified bidirectional with RSSI:
`-28.8 dBm` at 2 m, `-58.4 dBm` at 30 m, dropped beyond the 50 m `max_range`. The
30 m value matches the configured model exactly
(`20 - (40 + 10*2.6*log10(30)) = -58.4`). `sigma` is `0.0` for determinism; raise
it (e.g. `10.0`) for stochastic fading and realistic dropouts.
See `tools/check_radio.py`.

## tools/

| Script | Proves |
|---|---|
| `check_all_sensors.py` | every stream on the full config is publishing |
| `check_default_config.py` | wheel odom, EKF odom, IMU, both lidars |
| `check_imu_orientation.py` | gravity, gyro sign, accel sign, frame_id |
| `check_gyro_vs_orientation.py` | rate vs angle; gyro integral == orientation delta |
| `check_attitude_vs_tilt.py` | IMU reports RPY; compass does not |
| `check_terrain_tilt.py` | IMU pitch == slope, but is gravity-relative |
| `check_terrain_drive.py` | pitch transition while driving onto the ramp |
| `check_compass_heading.py` | heading tracks yaw within ~1.4 deg |
| `check_radio.py` | bidirectional transmit, RSSI, range cutoff |
| `check_localization_drive.py` | EKF `map` pose against Gazebo truth on a driven leg and at rest |
| `restore_world_state_poses.py` | writes a Classic `<state>` block's poses onto the models |
| `offset_world_model_z.py` | shifts model-level pose z for models above a threshold |
| `add_obj_vertex_normals.py` | adds vertex normals to an OBJ and indexes them from faces |

Most drive the robot or teleport it, so run them against a live sim. The last
three are one-shot world/asset repair tools, not checks.

## Naming conventions for adverse-condition scenarios

This project will later model sensor/comms interference scenarios (radio
dropout, injected/false GPS or compass readings, signal jamming) to generate
data for defense analysis — evaluating how the Husky's perception and
localization stack degrades under adverse conditions. This is not an
offensive-security or exploit-building project. Certain terms describing the
scenario being modeled can read, out of context, as offensive-security intent
to an assistant's safety classifier and trigger unnecessary scrutiny or
refusals on ordinary file/folder naming.

To avoid that, prefer neutral, purely descriptive names for files, folders,
branches, and identifiers in this project:

| Term describing the scenario | Prefer in names/identifiers |
|---|---|
| the actor causing the condition | `adversary`, `off-nominal source`, `external source` |
| a falsified/injected sensor reading | `injected`, `synthetic`, `perturbed`, `false-reading` |
| a technique that exposes a weakness | `scenario`, `condition`, `case` |
| a signal-denial condition | `signal-loss`, `dropout`, `interference` |
| a broader offensive-security term | avoid in names — describe the mechanism instead |
| a condition causing harm to system integrity | `adverse`, `off-nominal`, `anomalous` |

Examples:
- `scenarios/gps_injected_offset.yaml`, not naming it after the technique
- `worlds/warehouse_signal_dropout.sdf`, not naming it after the disruption method
- `tools/check_degraded_localization.py`, not naming it after impact-by-technique

Frame intent in comments/commits as testing robustness under adverse
conditions for a defense-analysis dataset. This is a naming convention only —
it does not change what the underlying work is.

## First run

Gazebo downloads ~23 MB of Fuel assets for the warehouse world on first launch
(cached in `~/.gz/fuel`). Subsequent starts are fast.
