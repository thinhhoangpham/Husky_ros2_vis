# config/map_server.yaml
#
# Serves the park prior obstacle map (maps/park_map.yaml) to nav2's costmaps.
a200_0000:
  map_server:
    ros__parameters:
      use_sim_time: true
      frame_id: "map"
      topic_name: "/a200_0000/map"
      yaml_filename: "/home/thinhpham/Documents/Husky_viz/maps/park_map.yaml"
